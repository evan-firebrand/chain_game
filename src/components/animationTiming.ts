export const ANIM = {
  flyPerTile: 180,
  flyStagger: 40,
  popDuration: 200,
  shakeDuration: 220,
  shakeThreshold: 64,
  dropDuration: 280,
  dropColStagger: 30,
  spawnDuration: 300,
  spawnColStagger: 25,
};

// Phase start times (ms) relative to commit.
export const PHASE = {
  flying: 0,
  pop: 380,
  dropping: 520,
  spawning: 680,
  idle: 980,
};
